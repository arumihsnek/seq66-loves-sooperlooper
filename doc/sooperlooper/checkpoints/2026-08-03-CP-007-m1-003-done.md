# Checkpoint: M1-003 observed-state cache completed

## Checkpoint ID: `CP-007`
## Checkpoint date: 2026-08-03

## Objective
M1-003: Build an observed-state cache that separates desired from observed
SooperLooper state, tracks freshness per field, distinguishes zero from
absent, coalesces meters/position, and provides immutable snapshots for
UI/performer consumers.

## Completed
- PR #5 merged to fork-main (merge commit `da8ec5cb1a6afc392e17848154af5c4d8bf038f5`)
- `sooperlooper_observed_cache` class implemented with:
  - `observed_field<T>` template: value + present flag + timestamp_us
  - `loop_observed_state`: 11 per-loop fields (state, next_state, waiting, loop_len, loop_pos, cycle_len, rate_output, channel_count, is_soloed, in_peak_meter, out_peak_meter)
  - `global_observed_state`: 5 global fields (tempo, eighth_per_cycle, sync_source, global_cycle_len, global_cycle_pos)
  - `apply()`: mutex-protected event application, unknown controls rejected
  - `snapshot()`: immutable deep copy under lock
  - `clear()`: generation-reset hook for M1-004
  - `dirty()`, `loop_count()`, `is_present()` accessors
- 14 test groups all pass with -Werror compilation
- Co-consulted with codex-senior-consult: plan mode (data structure decision) + merge-gate (accept)

## Verification
- Audio integration core run `30777160194` job `91574707581`: PASS (compile + all 14 test groups)
- Local: `.ci/bin/sooperlooper_observed_state_test` PASS — all 14 test groups passed
- Existing audio tests unchanged and passing (no regression)
- Consultant verdict: accept, safe_to_merge=true, 0 blocking findings

## Current state
M1-003 is complete and merged. Active task is M1-004 (engine generation
and stale feedback).

## Risks and unresolved questions
- Loop index reuse across engine generations (M1-004 concern)
- Latest-value cache doesn't preserve transition history (receiver dispatch handles it)
- Unknown controls from future SooperLooper versions safely rejected at typed boundary

## Next executable action
Begin M1-004 (engine generation and stale feedback). Create a new
feature branch from fork-main and open a draft PR.

## Open first
- `doc/sooperlooper/WORK-QUEUE.md` — M1-004 entry
- `doc/sooperlooper/TRACEABILITY.md` — STATE-005, ALLOC-001, FAIL-001 rows

## Safe reference point
Merge commit `da8ec5cb1a6afc392e17848154af5c4d8bf038f5` on fork-main.
All tests passing. Control files updated.
