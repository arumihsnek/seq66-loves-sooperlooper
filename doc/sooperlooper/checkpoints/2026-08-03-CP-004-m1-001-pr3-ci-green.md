# Checkpoint CP-004 — M1-001 published and in review

Date: 2026-08-03
Author: OpenAI assistant, GitHub supervision
Branch: `feature/m1-001-typed-osc-protocol`
Target branch: `fork-main`
Draft pull request: `#3`
Implementation head verified by fast CI: `1fc306e48c4d5af80cfcf9623b5a0e9b4bf10722`
Pre-checkpoint control head: `e105d67448de0eb52b8f15348b829e6ab5e0ae5c`
Status: M1-001 implementation published; task moved to `review`

This file is immutable after publication. Later sessions create a new
checkpoint and update `checkpoints/CURRENT.md`.

## Objective

Close the publication and CI-validation step for M1-001 after correcting the
shell line continuations in `.github/workflows/audio-core.yml`, then make the
repository control plane accurately describe the real GitHub state.

The checkpoint distinguishes fast CI that ran on the M1-001 implementation
head from the pinned real-engine smoke, which did not trigger for this PR diff
because of its path filters.

## Completed

- PR #1, the bootstrap PR, is merged into `fork-main` at merge commit
  `cb6929c044cab6e60b99b8aa653e220b4c40a817`.
- PR #2, the bounded `codex-senior-consult` workflow policy, is merged into
  `fork-main` at merge commit
  `c2999d873d2b169a01521f225e27bee3156af3ee`.
- GitHub's default branch is `fork-main`; `master` remains the upstream mirror
  line.
- Draft PR #3 is open from `feature/m1-001-typed-osc-protocol` to
  `fork-main` and is mergeable.
- The workflow compile commands were corrected so the shell receives complete
  `g++` commands rather than a broken command followed by a literal `:` token.
- `PROJECT-MANIFEST.json` now records `fork-main` as the actual default branch,
  PR #3 as the active PR and M1-001 as `review`.
- `WORK-QUEUE.md` now records the draft PR, implementation head and exact CI
  evidence.
- No M1-002 implementation has started.

## Verification

### PR and implementation identity

- PR: `#3`, title `Audio: add typed SooperLooper protocol identifiers`.
- Base: `fork-main` at `c2999d873d2b169a01521f225e27bee3156af3ee`.
- Implementation head tested by the fast workflows:
  `1fc306e48c4d5af80cfcf9623b5a0e9b4bf10722`.
- PR state observed: open, draft, mergeable.

### Project control plane

- Workflow: `Project control plane`.
- Run: `30770456444`.
- Job: `91556592918` (`validate-control-plane`).
- Result: PASS.
- `Validate manifest JSON`: PASS.
- `Validate control-file consistency`: PASS.

### Audio integration core

- Workflow: `Audio integration core`.
- Run: `30770456443`.
- Job: `91556592912` (`compile-and-test`).
- Result: PASS.
- Compile audio model test: PASS.
- Compile headless OSC contract test: PASS.
- Compile typed protocol test: PASS.
- Run audio model test: PASS.
- Run headless OSC contract test: PASS.
- Run typed protocol test: PASS.

This confirms the corrected workflow syntax and the three focused binaries on
implementation head `1fc306e48c4d5af80cfcf9623b5a0e9b4bf10722`.

### Real SooperLooper smoke

The `Real SooperLooper headless smoke` workflow did not trigger on PR #3's
implementation head. Its pull-request path filter includes only:

- `tests/audio/sooperlooper_real_engine_smoke.cpp`;
- `.github/workflows/sooperlooper-real-engine.yml`;
- `doc/sooperlooper/OSC-CONTROL-AND-FEEDBACK.md`;
- `doc/sooperlooper/HEADLESS-TESTING.md`.

PR #3 did not change those paths. Therefore this checkpoint does not claim a
new real-engine PASS for the PR #3 head.

The latest separately verified pinned-engine evidence remains:

- workflow run `30756422662`;
- job `91519250698` (`real-engine-smoke`);
- conclusion PASS;
- pinned SooperLooper
  `c5e22ce76ae9a6b358fe7d85720c61dfc5af8bec` / 1.7.9;
- build, JACK dummy startup, engine startup, bidirectional smoke and cleanup all
  passed.

## Current state

Completed phase: `phase-0-project-contract`.

Current phase: `phase-1-protocol-core`.

Active task: `M1-001` — typed protocol identifiers.

Task status: `review`.

M1-001 now has published code, a draft PR and green fast CI evidence. The task
is not `done`: PR review and merge remain human-gated, and the real-engine
workflow has not executed on the PR #3 head because it is outside the workflow's
path scope.

M1-002 remains `ready` but must not begin on this branch. It should start from a
fresh task branch after PR #3 is reviewed and merged into `fork-main`.

## Risks and unresolved questions

- The real-engine smoke proves the pinned bootstrap protocol probe, not the new
  typed mapping module directly. This is acceptable for the current path-filter
  policy because M1-001 changes no engine probe, callback contract or pinned
  engine workflow, but it must not be described as a fresh PR #3 run.
- Per-control ranges remain wire-level acceptance metadata. Later tasks must not
  infer UI safety, operation semantics or confirmation behavior solely from
  those ranges.
- PR #3 contains control-only commits after implementation head `1fc306e4`.
  Fast audio CI evidence remains attached to the unchanged implementation; the
  final branch head still requires a green project-control run after the
  checkpoint pointer update.
- M1-001 must not be marked `done` or used as authorization to begin M1-002 until
  the PR review/merge gate is completed.

## Next executable action

1. Update `checkpoints/CURRENT.md` to point to CP-004.
2. Update PR #3 with the exact run/job evidence and the real-engine path-filter
   distinction.
3. Confirm the final control-only branch head receives a green
   `Project control plane` run.
4. Review PR #3 for merge readiness without starting M1-002.
5. Merge PR #3 only after explicit human authorization; then create a fresh
   M1-002 branch from updated `fork-main` and leave a new checkpoint.

## Open first

A new session should open, in order:

1. `PROJECT-MANIFEST.json`;
2. `doc/sooperlooper/checkpoints/CURRENT.md`;
3. this checkpoint;
4. PR #3 metadata, diff and latest checks;
5. only the M1-001 section of `WORK-QUEUE.md`;
6. affected `TRACEABILITY.md` rows;
7. protocol source and tests only if review finds a concrete issue.

Do not start by loading the inherited Seq66 TODO, complete manual or broad
source directories.

## Safe reference point

Repository: `arumihsnek/seq66-loves-sooperlooper`.

Stable integration branch before PR #3:
`fork-main` at `c2999d873d2b169a01521f225e27bee3156af3ee`.

M1-001 implementation head with green fast CI:
`1fc306e48c4d5af80cfcf9623b5a0e9b4bf10722`.

Control-plane state immediately before this checkpoint:
`e105d67448de0eb52b8f15348b829e6ab5e0ae5c`.

Required fast evidence:

- project control run `30770456444`, job `91556592918`, PASS;
- audio core run `30770456443`, job `91556592912`, PASS.

Latest separate real-engine evidence:
run `30756422662`, job `91519250698`, PASS.

Recovery must use repository refs and GitHub CI, not this narrative alone.
