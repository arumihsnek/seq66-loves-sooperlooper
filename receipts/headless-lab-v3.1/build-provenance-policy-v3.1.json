{
  "batch_id": "BATCH-20260805T165044Z-DOGFOOD004-LAB-V3_1",
  "canonical_sources": [
    {
      "blob_sha_at_candidate": "6f07457cd38a81c425b4ab080aeee46bf0c0e7de",
      "path": "tests/integration/virtual_studio/audio/synthetic_source.c"
    },
    {
      "blob_sha_at_candidate": "c6dd79fae6c49aea907b1e4814b0cc9d8d9f0994",
      "path": "tests/integration/virtual_studio/audio/deterministic_capture.c"
    }
  ],
  "forbidden_inputs": [
    "tests/integration/virtual_studio/audio/synthetic_source",
    "tests/integration/virtual_studio/audio/deterministic_capture",
    "any ELF under .autonomy-recovery preservation trees",
    "any ELF from the failed LAB worktrees"
  ],
  "leaf_provenance_receipt_required_fields": [
    "source commit + blob sha",
    "exact compile command",
    "toolchain version",
    "start/end UTC timestamps",
    "output sha256",
    "transcript of the actual build (stdout/stderr)"
  ],
  "policy": [
    "controller IGNORES binaries generated by the leaf when verifying provenance",
    "controller creates an EMPTY build dir and recompiles from canonical sources",
    "compile command captured: gcc -std=c11 -O2 -o <out> <src> -ljack -lm -lpthread",
    "capture compiler path (readlink -f), compiler --version, environment, source blob SHAs, source sha256, timestamps",
    "output stat + output sha256 + ELF metadata + dynamic dependencies captured",
    "controller executes acceptance tests with the FRESHLY BUILT outputs",
    "provenance not reproducible -> REJECT (provenance_not_demonstrated)",
    "any ELF tracked in a result commit -> REJECT",
    "hash equality with quarantine is NEVER used alone as proof of copy or of rebuild",
    "no v3.1 artifact treats hash equality OR hash difference as sufficient proof of copying or of rebuild; only a controlled rebuild from canonical sources with captured commands/environment/toolchain/timestamps demonstrates provenance"
  ],
  "schema": "build-provenance-policy/v3.1"
}
