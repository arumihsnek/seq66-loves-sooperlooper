# Checkpoint: M1-004 engine generation tracking completed

## Checkpoint ID: `CP-008`
## Checkpoint date: 2026-08-03

## Objective
M1-004: Implement engine-generation tracking so that loop indexes from a
previous engine restart cannot mutate current observed state.

## Completed
- PR #6 merged to fork-main (merge commit `bf0300072a23d7e1efa57565c4abb4643577d81b`)
- `uint64_t` generation token added to `sooperlooper_observed_cache`
- `set_generation()`: clears all cached state and advances generation atomically
- `apply()`: accepts optional `event_generation` parameter; rejects stale events
- `snapshot_data.generation`: reflects current generation in snapshots
- Backward compatible: default `event_generation=0` accepts any generation
- 6 new test groups (20 total): generation lifecycle, stale rejection, matching acceptance, multiple resets

## Verification
- Audio integration core run `30778132565`: compile-and-test PASS (20 test groups)
- Local: `.ci/bin/sooperlooper_observed_state_test` PASS — all 20 test groups
- Existing audio tests unchanged (no regression)
- Co-consulted with codex-senior-consult (plan mode): uint64 counter confirmed, explicit reject for stale events

## Current state
M1-004 is complete and merged. Active task is M1-005 (ping, discovery
and subscriptions).

## Risks and unresolved questions
- Loop index UUID-to-index mapping deferred to later task
- Process-local counter not globally unique across engine recreation

## Next executable action
Begin M1-005 (ping, discovery and subscriptions). Create a new
feature branch from fork-main and open a draft PR.

## Open first
- `doc/sooperlooper/WORK-QUEUE.md` — M1-005 entry
- `doc/sooperlooper/TRACEABILITY.md` — OSC-005, STATE-006, FAIL-002 rows

## Safe reference point
Merge commit `bf0300072a23d7e1efa57565c4abb4643577d81b` on fork-main.
All 20 tests passing. Control files updated.
